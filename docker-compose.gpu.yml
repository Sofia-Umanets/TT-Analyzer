# Оверрайд для GPU — требует nvidia-container-toolkit на хосте.
#
# Запуск: docker compose -f docker-compose.yml -f docker-compose.gpu.yml up -d

services:
  backend:
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
